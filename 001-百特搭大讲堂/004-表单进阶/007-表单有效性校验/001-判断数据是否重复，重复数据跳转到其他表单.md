---
title: "判断数据是否重复，重复数据跳转到其他表单"
node_type: "doc"
source_url: "https://baiteda.yuque.com/czwxoe/wuacuf/gkk9mmaa1kqbxcyg"
visible: true
slug: "gkk9mmaa1kqbxcyg"
doc_id: 187621900
updated_at: "2024-09-24T03:56:38.000Z"
breadcrumb:
  - "百特搭大讲堂"
  - "表单进阶"
  - "表单有效性校验"
  - "判断数据是否重复，重复数据跳转到其他表单"
---
第一种：菜单操作是打开新页面

​

菜单对应表单：绑定某个输入项的值改变事件


```javascript
export async function CustomerInput (payload) {
      var res = await utils.querySvc({
        "app_id": "app_k1shhqtno9",
        "query":{//查多服务是需要传递
        "page_index":1,
        "page_size":10,
        "query_criteria":[
            {
                "column_name":"institution_name",
                "query_type":0,
                "value":[ctx.getState('aCwFLalQUF') ]
            }
        ],
    },
    "svc_code": "institution_8jign6bo_selectMore"
    });

         if( res.data.total == 0){

         }else{
           const result = await utils.confirm('警告：', '该客户已存在，是否跳转到共同维护页面?')
           if(result){
                // 重定向到另一个页面 form_tbjqssm91t  ，并带入参数customerId
                var  customerId = res.data.value[0].uid;
                const router = utils.getRouter();
                //router.push('/notFound/404')
                window.location.href='form_tbjqssm91t?institution_id='+customerId;
                // router.back()
                // router.forward();
                //const href = router.resolve({ name: 'users', params: { customerId: customerId }, hash: '#bio' })


           }
         }

}
```


​

新页面：


```javascript
export async function pageOnload (payload) {
   //从request的url请求中获取参数
   let temp2 = window.location.href.split('?')[1];
   let pram2 = new URLSearchParams('?'+temp2);
   var institution_id = pram2.get('institution_id')
   //调用查单，填充表单数据
   var getOne = await utils.querySvc({
        "app_id": "app_vxtpmw49kc",
        "svc_code": "institution_8jign6bo_selectOne",
        "save_datas": {
             "uid": institution_id
        }
    });
  ctx.setState('POWUVRMc7k',getOne.data.value.institution_name);
  ctx.setState('MT8rxAVeCi',getOne.data.value.key_responsible_person);
  ctx.setState('wbKT25V66j', institution_id);
}
```


​

​

​

​

​

​
