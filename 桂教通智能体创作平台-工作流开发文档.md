# 智能体创作平台 - 工作流开发文档

## 1. 平台概述

### 1.1 技术架构

| 项目 | 技术选型 |
|------|----------|
| 基础平台 | Dify (SELF_HOSTED 自部署版) |
| 前端框架 | Next.js (App Router) |
| 工作流编辑器 | React Flow |
| UI 组件库 | 自定义组件系统 (data-theme="light") |
| API 前缀 | `/console/api` (管理API) / `/api` (公共API) |
| 市场插件 | `https://marketplace.dify.ai` / 技能广场 |

### 1.2 平台配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 文本生成超时 | 60000ms | 单次文本生成最大时长 |
| 最大工具数 | 10 | 单个节点最多挂载工具数 |
| 最大并行数 | 10 | 并行执行最大分支数 |
| Top-K 最大值 | 10 | 知识检索返回最大条数 |
| 分段最大Token | 4000 | 知识库索引分段Token上限 |
| 循环节点最大数 | 100 | 单个工作流循环节点上限 |
| 最大迭代次数 | 5 | 迭代节点最大循环次数 |
| 网页阅读器 | jinareader / firecrawl / watercrawl | 支持的网页抓取工具 |

---

## 2. 工作流类型

平台支持两种工作流类型：

- **Chatflow（高级对话流）**：适用于多轮对话场景，包含对话记忆、变量传递等能力
- **Workflow（标准工作流）**：适用于单次执行的任务型场景

当前工作流为 **Chatflow** 类型（URL参数 `category=advanced-chat`）。

---

## 3. 节点类型详解

节点面板中的节点按类别组织：

| 类别 | 节点类型 |
|------|----------|
| LLM | 大模型 |
| 知识与智能体 | 知识库、智能体 |
| 问题理解 | 问题分类 |
| 逻辑 | 条件分支 |
| 逐步 | 循环 |
| 转换 | 代码执行、变量聚合、文档提取、变量赋值、参数提取 |
| 工具 | 网络请求、数组操作 |
| 其他 | 回复（End） |

---

### 3.1 开始节点（Start）

每个工作流必须有且只有一个开始节点，定义工作流的输入参数。

#### 系统内置变量

| 变量名 | 类型 | 说明 |
|--------|------|------|
| `sys.query` | String | 用户输入的查询文本 |
| `sys.files` | Array[File] | 用户上传的文件列表 |
| `sys.user_language_count` | Boolean | 用户语言计数 |
| `sys.conversation_id` | String | 当前对话ID |
| `sys.user_id` | String | 用户唯一标识 |
| `sys.app_id` | String | 当前应用ID |
| `sys.workflow_id` | String | 当前工作流ID |
| `sys.workflow_run_id` | String | 当前工作流运行ID |

#### 自定义输入字段

可在开始节点中添加自定义输入字段，支持类型：
- String（字符串）
- Number（数字）
- Boolean（布尔值）
- Array[File]（文件数组）
- Object（对象）

---

### 3.2 大模型节点（LLM）

核心节点，用于调用大语言模型进行文本生成。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **模型** | 选择LLM模型，如 DeepSeek-V3 (CHAT)、DeepSeek-R1 等 |
| **参考信息** | 可设置变量值作为上下文参考 |
| **系统提示词** | 定义模型的角色和行为规则 |
| **记忆** | 对话记忆配置，支持内置/用户两种模式 |
| **记忆窗口** | 控制历史对话保留轮数（如15轮） |
| **视觉** | 多模态视觉能力开关 |
| **输出变量** | 定义模型输出的变量名和类型 |
| **结构化输出** | 启用后可输出 JSON 格式的结构化数据 |

#### 提示词语法

- 输入 `{` 插入变量引用
- 输入 `/` 插入提示内容块
- 支持多段提示词组合

#### 模型信息显示

节点上显示：模型名称 + 模型类型（CHAT）+ 功能图标（视觉、结构化输出等）

---

### 3.3 回复节点（End/Reply）

工作流的终止节点，定义最终输出内容。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **回复内容** | 定义回复文本，支持变量插值 |
| **回复格式** | 支持纯文本、Markdown、代码块、富文本 |
| **输出变量** | 引用上游节点的输出变量 |

回复节点上显示：回复内容预览 + 引用的上游变量（如 `大模型 text`）

---

### 3.4 知识库节点（Knowledge Retrieval）

从知识库中检索相关信息，用于增强LLM的回答质量。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **查询变量** | 检索查询的输入变量（如 `sys.query`，String类型） |
| **知识库** | 连接已创建的知识库，支持多知识库联合检索，点击"+"按钮添加 |
| **召回设置** | 可配置检索模式（语义检索 / 全文检索 / 混合检索） |
| **元数据过滤** | 默认禁用，启用后可基于元数据条件过滤检索结果 |
| **输出变量** | 检索结果输出，供下游节点使用 |

---

### 3.5 智能体节点（Agent）

具有自主推理和多步工具调用能力的智能体节点。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **智能体策略** | 必填，选择智能体的推理和工具调用策略。配置完成后自动加载剩余配置项 |
| **输出变量** | 智能体输出结果 |

> 策略将影响多步工具推理的机制。策略未设置时节点显示"智能体策略未设置"。

---

### 3.6 问题分类节点（Question Classifier）

对用户输入进行意图分类，根据分类结果走不同分支。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **模型** | 必填，选择用于分类的LLM模型（如 DeepSeek-V3 CHAT） |
| **输入变量** | 必填，分类的输入源（如 `sys.query`，String类型） |
| **视觉** | 多模态视觉能力开关 |
| **分类** | 必填，至少两个分类。每个分类需填写主题内容描述。支持"添加分类"增加更多分支 |
| **高级设置** | 额外的分类配置选项 |
| **输出变量** | 分类结果输出 |

#### 分支行为

每个分类生成一个独立的下游分支，节点下方显示"分类 1 → 选择下一个节点"、"分类 2 → 选择下一个节点"等。

---

### 3.7 条件分支节点（IF/ELSE）

根据条件判断走不同分支，支持多条件组合。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **IF** | 主条件，点击"添加条件"设置。支持多种运算符（等于、包含、大于、小于等）和 AND/OR 逻辑组合 |
| **ELIF** | 简化条件分支（可选） |
| **ELSE** | 当 IF 条件不满足时执行的逻辑。描述："用于定义当 if 条件不满足时应执行的逻辑。" |

#### 分支行为

- **IF 分支**：条件满足时执行
- **ELSE 分支**：条件不满足时执行

---

### 3.8 循环节点（Iteration/Loop）

对数组中的每个元素执行相同的操作流程。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **循环变量** | 设置循环范围内的变量 |
| **循环终止条件** | 点击"添加条件"设置终止条件 |
| **最大循环次数** | 控制最大迭代次数（平台上限5次） |

---

### 3.9 代码执行节点（Code Execution）

执行自定义代码逻辑，支持 Python 和 Node.js。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **输入变量** | 设置变量值，可引用上游节点的输出变量 |
| **代码语言** | 支持 PYTHON3（默认） |
| **代码编辑器** | 内置代码编辑区域 |
| **输出变量** | 必填，定义返回变量的类型（默认 String） |

#### 功能说明

- 支持 Python 3.x 和 JavaScript (Node.js 18+)
- 适用于数据转换、格式化、计算等场景

---

### 3.10 变量聚合节点（Variable Aggregator）

将多个并行分支的变量聚合为一个统一输出。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **变量赋值** | 添加需要赋值的变量 |
| **聚合分组** | 设置聚合的分组策略 |

---

### 3.11 文档提取节点（Document Extractor）

从文件中提取文本内容，供下游节点使用。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **输入变量** | 必填，接受 File 或 Array[File] 类型 |
| **输出变量** | 提取后的文本内容 |

---

### 3.12 变量赋值节点（Variable Assigner）

将上游节点的输出赋值给指定变量。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **变量** | 点击"+"按钮添加需要赋值的目标变量 |

---

### 3.13 参数提取节点（Parameter Extraction）

使用LLM从文本中提取结构化参数。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **模型** | 必填，选择LLM模型（如 DeepSeek-V3 CHAT） |
| **输入变量** | 必填，设置变量值作为提取来源 |
| **视觉** | 多模态视觉能力开关 |
| **提取参数** | 必填，可"从工具导入"参数定义。初始显示"提取参数未设置" |
| **指令** | 提示词编辑器，支持变量插入（`{`）和提示内容块（`/`） |
| **高级设置** | 额外配置选项 |

---

### 3.14 网络请求节点（HTTP Request）

发送 HTTP 请求调用外部 API。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **API** | 必填，设置请求URL，输入变量时键入"/" |
| **鉴权** | 认证方式，默认"无" |
| **导入cURL** | 支持导入 cURL 命令快速配置请求 |
| **请求方法** | 支持 GET / POST / PUT / DELETE 等 |
| **HEADERS** | 键值对形式设置请求头，支持变量插入 |
| **PARAMS** | 键值对形式设置URL参数，支持变量插入 |
| **BODY** | 请求体，支持格式：none / form-data / x-www-form-urlencoded / JSON / raw / binary |
| **超时设置** | 请求超时时间配置 |
| **输出变量** | 响应数据输出 |

---

### 3.15 数组操作节点（Array Operation）

对数组类型数据进行过滤、取值和排序操作。

#### 配置项

| 配置项 | 说明 |
|--------|------|
| **输入变量** | 必填，接受 Array 类型变量 |
| **过滤条件** | 按条件筛选数组元素 |
| **取第 N 项** | 获取指定索引的元素 |
| **取前 N 项** | 获取前N个元素 |
| **排序** | 对数组元素排序 |
| **输出变量** | 操作后的数组输出 |

---

## 4. 工具插件（Tool Plugins）

节点面板中的"工具插件"选项卡提供可直接挂载到工作流中的工具。

### 4.1 工具分类

| 分类 | 说明 |
|------|------|
| 全部 | 显示所有可用工具 |
| 插件 | 平台内置插件 |
| 自定义 | 用户自定义工具 |
| 工作流 | 可作为工具调用的其他工作流 |

### 4.2 内置工具

| 工具名称 | 功能说明 |
|----------|----------|
| **Audio** | 音频处理工具 |
| **代码解释器** | 代码执行与解释工具 |
| **时间** | 时间相关操作工具 |
| **网页抓取** | 网页内容抓取工具 |

---

### 4.3 技能广场（Skill Marketplace）

技能广场是平台的插件市场，可通过 `/caih-home` 路径访问，用于查找和安装更多工具插件。技能广场地址：`https://agent.gjt-smart.com/caih-home`

技能广场包含"热门技能"和完整技能列表，安装后可直接在工作流的工具插件面板中使用。

#### 4.3.1 文档与文件处理

| 技能名称 | 功能说明 |
|----------|----------|
| **doc处理插件** | 文本内容生成 Word 文档 |
| **DOC** | 将 Markdown 文本转换为 Microsoft Word DOCX 文档 |
| **PDF处理工具** | PDF 文件处理工具 |
| **PDF文件OCR识别转Markdown格式** | 使用 GPT-4o 的 OCR 功能从 PDF 文件中提取文本并转换为格式良好的 Markdown，保留文档结构 |
| **通用文档解析** | 专为 LLM 下游任务设计，识别文档或图片中的文字信息，智能还原阅读顺序，输出标准化 Markdown。支持 OCR 识别，覆盖 10+ 种版式，52+ 种语言 |
| **Markdown 转换器** | 导出 Markdown 为 DOCX, PPTX, XLSX, PDF, PNG, HTML, MD, CSV, JSON, JSONL, XML，代码块可导出为 Python/JS/Bash 等脚本文件 |
| **Excel工具箱** | 将 Excel 转换为 JSON 及其反向转换 |
| **压缩** | 压缩多模态文件 |
| **OCR识别服务** | 支持 PDF、图片等文件的 OCR 识别服务工具 |
| **百度OCR文字识别** | 百度 OCR 文字识别插件 |
| **Json修复** | 修复损坏的 JSON 字符串 |
| **Base64 编码解码** | 支持纯文本、二进制文件、图片文件与 Base64 文本的互相转换 |
| **移除Markdown** | 移除 Markdown 语法 |
| **大模型提示词之美** | 大模型提示词创作的艺术与技巧 |

#### 4.3.2 图像与多媒体

| 技能名称 | 功能说明 |
|----------|----------|
| **图片处理插件** | 图片处理插件，文生图工具 |
| **图像工具** | Dify 平台的图像生成插件，支持文本转图像、涂鸦精修，以及创意海报自动生成 |
| **Stable Diffusion** | 可在本地部署的图片生成工具 |
| **Doubao Image and Video Generator** | 豆包图像/视频 API，支持火山引擎 AI 图像生成 |
| **图片下载器** | 从 URL 下载图片 |
| **FFmpeg** | 使用 FFmpeg 处理和转换视频和音频文件，提供视频信息获取、格式转换、剪切、压缩和提取音频 |
| **播客生成器** | 使用文字转语音服务生成播客音频 |
| **刮图人工智能** | 用于 LLM 驱动刮削的刮图人工智能集成 |

#### 4.3.3 搜索与信息获取

| 技能名称 | 功能说明 |
|----------|----------|
| **Firecrawl** | Firecrawl API 集成，用于网页爬取和数据抓取 |
| **Tavily** | 强大的原生 AI 搜索引擎和网页内容提取工具，提供高相关性搜索结果和网页原始内容提取 |
| **安思派联网搜索** | 面向 AI 的下一代实时智能搜索引擎，支持 Search Agent，理解上下文、执行研究并提供综合性附带引用的答案 |
| **PubMed** | 生物医学文献搜索引擎 |
| **学术摘要索引** | 基于 abstracts-search 的语义搜索插件，可搜索 1.1 亿篇学术出版物 |
| **聚合数据** | 聚合数据 API 工具箱（天气、汇率等） |

#### 4.3.4 智能体与 MCP 生态

| 技能名称 | 功能说明 |
|----------|----------|
| **Agent 策略（支持 MCP 工具）** | 提供 Function Calling 和 ReAct 的 Agent 策略集合，支持 MCP SSE / StreamableHTTP 发现和调用工具 |
| **MCP转换工具（发布智能体为MCP服务）** | 将 Dify 工具的 API 转换为 MCP 兼容 API |
| **MCP接入工具（接入外部MCP服务）** | 通过 HTTP with SSE 或 Streamable HTTP 传输方式使用 MCP 协议来发现和调用工具 |
| **使用时配置的MCP工具** | 自适应通过 Streamable Http 或 SSE 传输使用 MCP 协议来发现和调用工具，使用时配置 |
| **一个"思考"工具** | 通过逐步推理帮助非推理模型有条理地解决问题 |

#### 4.3.5 飞书生态

| 技能名称 | 功能说明 | 所需权限 |
|----------|----------|----------|
| **飞书日历** | 飞书日历操作 | calendar:calendar:read, calendar:calendar, contact:user.id:readonly |
| **飞书任务** | 飞书任务管理 | task:task:write, contact:user.id:readonly |
| **飞书消息** | 飞书消息发送 | im:message, im:message.group_msg |
| **飞书群机器人（LarkNotify）** | 向飞书群聊发送文本消息和卡片消息 | - |
| **飞书知识库** | 飞书知识库读取 | wiki:wiki:readonly |
| **飞书电子表格** | 飞书电子表格操作 | sheets:spreadsheet |
| **飞书云文档** | 飞书云文档操作 | docx:document, drive:drive, docs:document.content:read |
| **飞书多维表格** | 飞书多维表格操作 | bitable:app |

#### 4.3.6 可视化与图表

| 技能名称 | 功能说明 |
|----------|----------|
| **ECharts图表生成** | 生成可视化 ECharts 图表，支持柱状图、折线图、饼图等 |
| **AntV 可视化图表** | 基于 AntV 可视化解决方案的图表生成插件，支持 25+ 种常用可视化图表 |
| **脑图生成器** | 将 Markdown 文本转换为精美脑图，支持中心结构和左右结构两种布局 |

#### 4.3.7 数据库与数据处理

| 技能名称 | 功能说明 |
|----------|----------|
| **DB Schema RAG** | 自动创建数据库 schema 知识库构建 RAG，包含基于 schema RAG 的自然语言转 SQL 功能 |
| **数据库操作客户端** | 在工作流中提供读写数据库能力 |
| **表结构写入知识库** | 将数据库表结构写入知识库 |
| **智能问数工具** | 通过预建模（数据模型和语义模型）以自然语言方式查询数据 |

#### 4.3.8 云服务集成

| 技能名称 | 功能说明 |
|----------|----------|
| **阿里云SLS** | 查询阿里云 SLS 日志 |
| **阿里云AI安全护栏** | 阿里云安全提供的 AI 安全护栏 API 服务，保障大模型安全 |
| **阿里云工具集** | 集成阿里云一系列开放能力 |
| **腾讯云日志服务** | 腾讯云日志服务 CLS |
| **alist** | AList 文件列表程序，支持 Dropbox、阿里云盘、OneDrive、Google Drive 等多种存储 |

#### 4.3.9 通信与社交

| 技能名称 | 功能说明 |
|----------|----------|
| **微信公众号** | 微信公众号 API 集成，支持素材管理、草稿操作和消息发布 |
| **微信公众号对接助手** | 连接智能体与微信公众号 |
| **163SMTP邮件发送** | 通过 163 邮箱 SMTP 服务发送邮件 |
| **小支付** | 个人开发者工作对话流微信收款服务 |

#### 4.3.10 地图与地理

| 技能名称 | 功能说明 |
|----------|----------|
| **天地图** | 调用天地图接口，实现中国区域内地名搜索、地理编码、静态地图等功能 |
| **地理编码器/解码器** | 使用 OpenCage API 将坐标编码为地址，或将地址解码为坐标 |

#### 4.3.11 AI与NLP工具

| 技能名称 | 功能说明 |
|----------|----------|
| **百度翻译** | 使用百度进行翻译 |
| **正则表达式提取** | 正则表达式内容提取工具 |
| **随机生成器** | 生成随机数、字符串、UUID、质数等 |
| **数学工具** | 数学计算工具 |
| **求索时刻AI知识库组件** | 文档资料自动精准切片，超强多模态图表理解能力，问答准确率高、答案可精准溯源 |
| **易盾内容安全检测** | 对输入文本/图片进行内容安全检测，保障内容合规 |

#### 4.3.12 开发与运维

| 技能名称 | 功能说明 |
|----------|----------|
| **SSH命令执行** | 通过 SSH 使用密码或密钥认证在远程服务器上执行命令 |
| **Artifacts** | 聊天界面，可渲染 LLM 生成的 HTML 代码 |

#### 4.3.13 生活与娱乐

| 技能名称 | 功能说明 |
|----------|----------|
| **旅行规划助手** | 使用 Amadeus API 搜索航班和城市的旅行助手 |
| **一句古诗词** | 随机返回一句古诗词名句 |

---

## 5. 工作流编辑器功能

### 5.1 顶部工具栏

| 按钮 | 功能 |
|------|------|
| **预览** | 打开对话预览窗口，测试工作流 |
| **预检** | 检查工作流配置是否完整、有无错误 |
| **设置** | 工作流全局设置（名称、描述、模型默认值等） |
| **发布** | 将工作流发布为可用版本 |
| **立即对话** | 直接进入对话测试 |

### 5.2 画布控制

| 控件 | 功能 |
|------|------|
| **缩放控制** | 放大/缩小/适配画布，支持 100% 显示 |
| **撤销/重做** | 回退/前进操作历史 |
| **小地图** | 右下角缩略图导航 |
| **全屏** | 切换全屏编辑模式 |
| **自动排列** | 自动整理节点布局 |

### 5.3 节点操作

- **添加节点**：点击节点输出端的"+"按钮或拖拽添加
- **删除节点**：选中节点后按 Delete 键或右键菜单
- **复制节点**：Ctrl+C / Ctrl+V
- **拖拽连接**：从节点输出端拖拽到下一节点输入端
- **并行分支**：同一输出端可连接多个下游节点（最多10个）

---

## 6. 数据流架构

```
用户输入
    ↓
[开始节点] → 定义输入参数 (sys.query, sys.files, ...)
    ↓
[处理节点] → LLM / 知识检索 / 代码 / 条件分支 / 迭代 / 智能体 / 参数提取
    ↓                    ↓
    ↓              [并行分支] → 变量聚合
    ↓
[回复节点] → 输出最终结果给用户
```

### 变量传递机制

- 节点间通过**变量引用**传递数据
- 使用 `{variable_name}` 语法在提示词中引用变量
- 每个节点可定义输出变量供下游使用
- 支持系统变量（`sys.*`）和自定义变量

---

## 7. API 接口

### 7.1 管理 API

前缀：`/console/api`

| 端点 | 方法 | 说明 |
|------|------|------|
| `/console/api/apps/{app_id}` | GET | 获取应用信息 |
| `/console/api/apps/{app_id}/workflows` | GET | 获取工作流列表 |
| `/console/api/apps/{app_id}/workflow` | GET | 获取工作流定义 |

### 7.2 公共 API

前缀：`/api`

用于外部调用工作流执行对话。

---

## 8. 环境变量配置

通过页面顶部的"环境变量"按钮可配置：

- API 密钥
- 外部服务凭证
- 自定义环境变量

---

## 9. 发布与部署

### 9.1 发布流程

1. 完成工作流设计
2. 点击"预检"检查配置
3. 点击"预览"测试对话效果
4. 点击"发布"生成新版本
5. 发布后可通过 API 或嵌入方式使用

### 9.2 版本管理

- 每次发布生成新版本
- 支持版本回滚
- 未发布状态显示"未发布"标签

---

## 10. 开发注意事项

### 10.1 节点限制

- 单个工作流最多 10 个工具节点
- 并行分支最多 10 个
- 循环节点最多循环 5 次
- 循环节点总数不超过 100

### 10.2 模型调用

- 文本生成超时 60 秒
- 支持 CHAT 类型模型（如 DeepSeek-V3、DeepSeek-R1）
- 支持多模态视觉模型

### 10.3 知识库

- 分段 Token 上限 4000
- Top-K 最大值 10
- 支持多种网页抓取工具（jinareader、firecrawl、watercrawl）

### 10.4 安全

- API 调用需要认证（401 验证）
- 管理 API 和公共 API 使用不同前缀
- Sentry 错误监控已配置（当前 DSN 为空）
